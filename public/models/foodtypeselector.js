const db = require('./db'); //require the database

exports.addnewitem =  async(req, res) =>{
    console.log("inside function");
    // Storing DATA HERE to DATABASE

    // db.query('SELECT * FROM foodtype', (err, rows) => {
    //     if(err){
    //         throw err;
    //     }

    //     console.log("data got got");
    //     console.log(rows);
    // })


}